import { it, describe } from "vitest";

describe("profile services", () => {
  it.skip("should return user informations", async () => {
    // to:do;
    // don't forget to write beforeEach(), to create a new dependecy instace, instead of creating an instance in every test
  });
});
